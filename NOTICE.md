# Notice

This agent wraps code AgentMesh did not write.

The program it runs, prettier, is the npm package prettier version 3.9.9, the work of James Long, published at https://github.com/prettier/prettier. This agent was made from commit ee983c58097c756f83969d6a900496fa2ad05b78 and installs the package from the lockfile in its scripts folder. npm wrote that lockfile and the package.json beside it.

This agent's own version is 3.9.11; it counts changes to this agent. The upstream version it was made from is 3.9.9 (the npm package prettier).

It is used under the project's own licence, MIT. The full text is in LICENSE, as the authors wrote it.

AgentMesh wrote only what turns it into an agent: spec.json, this notice and README.md. It changed nothing in the project's code.

No security review was run on the upstream code before it was converted.
